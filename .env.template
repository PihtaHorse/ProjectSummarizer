# .env.template

# API key for Anthropic Claude models
# Required for counting tokens with Claude models
# Get your API key from: https://console.anthropic.com/settings/keys
#
# WARNING: By using this API key, your code content will be sent to Anthropic's servers
# for token counting. This is NOT a local operation. Do NOT use with confidential or
# proprietary code. For safe local token counting, use OpenAI (tiktoken) or Google
# (Gemini) models instead with the --count_tokens flag.
CLAUDE_API_KEY=your_claude_api_key_here
